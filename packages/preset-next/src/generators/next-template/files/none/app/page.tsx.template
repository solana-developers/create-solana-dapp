<% if (anchor.fileName !== 'none') { %>
import { <%= anchorProgramName.className %>IDL } from '@<%= npmScope %>/<%= anchorName.fileName %>';
<% } %>

export default async function Index() {
  return (
    <div>
<% if (anchor.fileName !== 'none') { %>
      <pre>{JSON.stringify(<%= anchorProgramName.className %>IDL, null, 2)}</pre>
<% } else { %>
      <div>Index</div>
<% } %>
    </div>
  )
}
